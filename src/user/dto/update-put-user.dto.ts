import { CreateUserDTO } from "./create-user.dto";

export class UpdatePutUserDTO extends CreateUserDTO { // Ao utilizaar o extends ele já insere automaticamente o conteudo do que está sendo extendido
}
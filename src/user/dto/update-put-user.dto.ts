import { CreateUserDTO } from "./create-user.dto";

export class UpdatePutUserDTO extends CreateUserDTO { // Ao utilizar o extends ele já insere automaticamente o conteudo do que está sendo extendido
}